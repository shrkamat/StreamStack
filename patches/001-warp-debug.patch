diff --git a/src/browser.ts b/src/browser.ts
index 213b243..7445b65 100644
--- a/src/browser.ts
+++ b/src/browser.ts
@@ -664,7 +664,7 @@ async function connect() {
     saveConnectionSettings(serverUrlInput.value, fingerprintUrlInput.value);
   } catch (error) {
     logger.error(
-      `Connection error: ${
+      `SKM Connection error: ${
         error instanceof Error ? error.message : String(error)
       }`,
     );
@@ -675,7 +675,7 @@ async function connect() {
 // Disconnect from the MOQ server
 function disconnect() {
   if (player) {
-    logger.info("Disconnecting from server");
+    logger.info("SKM Disconnecting from server");
     player.disconnect();
     resetUI();
   }
diff --git a/src/transport/client.ts b/src/transport/client.ts
index 535f49d..611363a 100644
--- a/src/transport/client.ts
+++ b/src/transport/client.ts
@@ -66,13 +66,15 @@ export class Client {
         fingerprint.value instanceof Uint8Array
           ? fingerprint.value
           : new Uint8Array(fingerprint.value as ArrayBuffer);
-      this.logger.debug(
+      this.logger.info(
         `Using certificate fingerprint: algorithm=${fingerprint.algorithm}, value=${Array.from(
           valueArray,
         )
           .map((b) => b.toString(16).padStart(2, "0"))
           .join("")}`,
       );
+    } else {
+      this.logger.warn("No certificate fingerprint available - connection may fail with self-signed certs");
     }
 
     this.logger.info(`Connecting to ${this.config.url}...`);
@@ -246,8 +248,7 @@ export class Client {
             );
           } catch (error) {
             this.logger.error(
-              `Error sending PublishNamespaceOk: ${
-                error instanceof Error ? error.message : String(error)
+              `Error sending PublishNamespaceOk: ${error instanceof Error ? error.message : String(error)
               }`,
             );
           }
@@ -258,8 +259,7 @@ export class Client {
               callback(msg.namespace);
             } catch (error) {
               this.logger.error(
-                `Error in publish namespace callback: ${
-                  error instanceof Error ? error.message : String(error)
+                `Error in publish namespace callback: ${error instanceof Error ? error.message : String(error)
                 }`,
               );
             }
@@ -288,10 +288,8 @@ export class Client {
               handlersForKind.delete(requestId);
             } catch (error) {
               this.logger.error(
-                `Error in message handler for kind ${
-                  msg.kind
-                } with requestId ${requestId}: ${
-                  error instanceof Error ? error.message : String(error)
+                `Error in message handler for kind ${msg.kind
+                } with requestId ${requestId}: ${error instanceof Error ? error.message : String(error)
                 }`,
               );
             }
diff --git a/webpack.config.mjs b/webpack.config.mjs
index 533c770..11a1873 100644
--- a/webpack.config.mjs
+++ b/webpack.config.mjs
@@ -47,13 +47,13 @@ export default (env, argv) => {
         title: "WARP Player",
         minify: isProduction
           ? {
-              collapseWhitespace: true,
-              removeComments: true,
-              removeRedundantAttributes: true,
-              removeScriptTypeAttributes: true,
-              removeStyleLinkTypeAttributes: true,
-              useShortDoctype: true,
-            }
+            collapseWhitespace: true,
+            removeComments: true,
+            removeRedundantAttributes: true,
+            removeScriptTypeAttributes: true,
+            removeStyleLinkTypeAttributes: true,
+            useShortDoctype: true,
+          }
           : false,
       }),
       new CopyWebpackPlugin({
@@ -75,7 +75,7 @@ export default (env, argv) => {
       },
       compress: true,
       port: 8080,
-      server: "http", // Changed from https to http
+      server: "https",
       client: {
         overlay: true,
       },
@@ -83,8 +83,8 @@ export default (env, argv) => {
     optimization: {
       splitChunks: isProduction
         ? {
-            chunks: "all",
-          }
+          chunks: "all",
+        }
         : false,
       minimize: isProduction,
     },
